from connect_db import db
from sqlalchemy.sql import text
# session =
""" Tento soubor pridává sloupec do tabulky students podobně jako kdyby jsme tento sloupec měli už v models_students"""
"""kombinace cisteho SQL s SQLAlchemy"""
# with db.connect() as conn:
#     sql_statement = text("ALTER TABLE students ADD class_name VARCHAR(30)")
#     conn.execute(sql_statement)
